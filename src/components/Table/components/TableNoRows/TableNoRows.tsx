import React from "react";

const TableNoRows = () => {
    return (
        <tr className="flex items-center h-full text-center">
            <td className="w-full text-xl font-bold mt-3 h-full">There is no data</td>
        </tr>
    );
};

export default TableNoRows;
