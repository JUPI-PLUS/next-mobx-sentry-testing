import FiltersSearchInput from "./components/FiltersSearchInput/FiltersSearchInput";

const Filters = () => {
    return <FiltersSearchInput name="name" placeholder="Search by name" autoFocus />;
};

export default Filters;
